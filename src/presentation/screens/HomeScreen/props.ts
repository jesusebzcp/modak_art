import {MainStackParamList} from '@pr/navigation/types';
import {StackScreenProps} from '@react-navigation/stack';

export interface HomeProps
  extends StackScreenProps<MainStackParamList, 'HOME'> {}
